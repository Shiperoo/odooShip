<odoo>
    <record id="view_pack_station_kanban" model="ir.ui.view">
        <field name="name">pack.station.kanban</field>
        <field name="model">pack.station</field>
        <field name="arch" type="xml">
            <kanban>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <field name="name"/>
                                <field name="hu_scanned"/>
                                <field name="pack_bench_id"/>
                                <field name="virtual_locations"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>
